{
  "name": "slackwater-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker: KV-cached proxy + cron warmer for NOAA/NWS/USNO tide data",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
